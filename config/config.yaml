storage:
  postgres:
    host: localhost
    port: 55631
    database: postgres
    user: postgres
    password: postgres

bot:
  method: poll # poll / webhook
  poll:
    interval: 3s
  webhook:
  messages:
    lifetime:
  verbose: yes
